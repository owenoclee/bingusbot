// Copy this file to config.ts and fill in your values.

// WebSocket server URL — update to your Tailscale hostname
export const WS_URL = "ws://localhost:8421";

// Shared secret for WS auth — must match server's WS_AUTH_TOKEN
export const AUTH_TOKEN = "CHANGE_ME";
